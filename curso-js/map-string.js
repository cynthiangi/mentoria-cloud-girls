const nomes = ["ana Julia", "Caio vinicius", "BIA silva"];

const nomesPadronizados = nomes.map((nome) => nome.toUpperCase()); //toUpperCase é uma função que retorna todos os caracteres em caixa alta

console.log(nomesPadronizados);
