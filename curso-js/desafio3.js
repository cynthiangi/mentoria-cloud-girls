/* 1) const listaCompras = ["Papel Higienico", "Amaciante", "Pão de Forma", "Carne", "Nuggets"];

for(item of listaCompras){
    console.log(item);
} 

2) const listaCompras = ["Papel Higienico", "Amaciante", "Pão de Forma", "Carne", "Nuggets"];

for(i = 0; i < listaCompras.length; i++) {
    console.log(i, listaCompras[i]);
} 

3) const numeros = [];

let soma = 0;
for(let i = 0; i < 10; i++) {
    numeros.push(i)
    soma += i;
}

console.log(`A soma dos elementos é: ${soma}`); 

4) const numeros = [];

for(let i = 0; i < 10; i++) {
    
    numeros.push(i)
    
}

console.log(`O menor número é ${numeros[0]} e o maior número ${numeros[9]}`); 

const numeros = [3, 8, 12, 5, 6, 10, 7, 2, 9, 14]

5) for(i = 0; i < 10; i++){
    if(numeros[i] % 2 == 0){ //para receber o valor do resto de uma divisão, utilizar %
        console.log(numeros[i]);
    }
}

6) const numeros = [3, 8, 12, 5, 6, 10, 7, 2, 9, 14];

let media = 0;

for(let i = 0; i < numeros.length; i++){
    media += numeros[i]/numeros.length;
}

console.log(media.toFixed(2)); */
